const favoriteFood = "tacos";

makeDinner(favoriteFood);

function makeDinner(whatToMake) {
  console.log(`I see you want to make ${whatToMake}.`);
  whaToMake = "salad";
  console.log(`I've decided to make ${whatToMake} instead.`);
}
